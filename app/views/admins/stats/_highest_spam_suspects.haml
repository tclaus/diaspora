= render partial: "admins/stats/stats_bar"

%h2
  User suspected for spam (en language only)
.row
  .col
    - if top_10_highest_spam_suspecs.empty?
      = "No suspicious users found."

    - else
      %h3
      = render partial: "admins/stats/person_list", locals: {top_10_highest_spam_suspecs: top_10_highest_spam_suspecs}
