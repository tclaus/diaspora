%table.table.table-striped
  %thead
    %tr
      %th{scope: "col"}
        User
      %th{scope: "col"}
        Count
  %tbody
    - list.rows.each do |author_id, count|
      %tr
        %th{scope: "row"}
          = "#{Person.find(author_id).diaspora_handle}"
        %th
          = "#{count}"
